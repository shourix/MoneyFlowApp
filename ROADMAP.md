# MoneyFlow ロードマップ

## ビジョン

「3〜6ヶ月先の将来予測」と「今週・今月の身近な予算管理」を両立するアプリ。
予測して → 記録して → 比較して → 補正する。このサイクルで予測精度が上がり続ける。

## 現状

- **予測アプリ**: 固定費・変動費の設定値から最大24ヶ月先の残高を予測
- **支出記録**: カテゴリ別予算に対する消化率・1日に使える金額の表示（予測には未反映）
- **データ保存**: localStorage（JSON文字列）、端末のみ
- **予測と実績が別世界** → 使い続けると実態とのギャップが広がる

## コンセプト: 実績フィードバックループ

```
予測（予算）→ 記録（実績）→ 比較 → 補正 → より正確な予測
```

例: 昼食代の予算 ¥10,000 → 実績 ¥8,000 → 差額 ¥2,000 を貯蓄に回す → 次月の予測を ¥8,000 に更新

---

## Phase 1: 通知機能（身近な予算の番人）

予測アプリだけでなく「今の使い方、大丈夫？」を教えてくれる存在にする。

### 通知ルール
- **月の半分（15日前後）を過ぎた時点で**: 予算消化率が60%を超えていたら「今月ペース速いかも」
- **週の予算超過**: 1週間の支出が（月予算÷4）を超えたら「今週使いすぎかも」
- **1日単位では通知しない**（ノイズになるため）
- **月末残高がマイナス予測**: 「今月の収支が赤字になりそうです」

### 実装方針
- まずは **ローカル通知**（Service Worker + Notification API）で実装
- サーバー不要、PWAのままで対応可能
- Capacitor化後はネイティブ通知プラグインに差し替え可能
- iOS PWAの通知到達率は70-85%（Androidは90-95%）

---

## Phase 2: 予測 vs 実績の可視化

- 月末に「予算 vs 実績」の比較サマリーを表示
- カテゴリごとに差額（余り / オーバー）を見せる
- ユーザーが手動で次月の予算を調整するきっかけを作る

## Phase 3: 実績ベースの予測補正

- 月が終わったら実績値で残高を確定
- 変動費の予測を直近Nヶ月の実績平均に自動提案（ユーザー承認制）
- 当月途中の予測を「使った分 + 残り日数の予測ペース」で動的計算

## Phase 4: 残高の自動連携

- 銀行口座の残高を手動 or API で取得し、予測の起点を実データに
- クレジットカードの利用明細を取り込み、支出を自動分類
- サブスク・アプリ課金の自動検出

### API連携の候補（将来）
- マネーフォワード / Zaim などの家計簿API
- 銀行オープンAPI（全銀協標準）
- クレジットカード明細（スクレイピング or API）
- Apple / Google のサブスク管理API

### カード反映の遅延問題
- クレカの利用明細は反映に数日〜数週間かかる
- → 手動入力 + 後からカード明細で突合・修正する運用が現実的
- → サブスクBoxのように「プリセット + 手動」でUXを磨く方向もあり

---

## データ保存の移行計画

### 現状の問題
localStorage はブラウザPWAでは問題ないが、Capacitor（WebView）では
アプリ強制終了時にデータ消失するリスクがある。

### 移行ステップ
1. **今**: localStorage のまま（ブラウザPWA用途では十分）
2. **アプリ化時**: Capacitor Preferences に移行（ネイティブKV保存、安全）
3. **データ量増加時**: IndexedDB or SQLite に移行（大量の支出記録に対応）

### 移行時の注意
- 既存ユーザーの localStorage データを新方式にマイグレーションする処理が必要
- エクスポート/インポート機能は引き続き JSON 形式で維持

---

## 設計方針

- **段階的に進める**: 各Phaseは独立してリリース可能にする
- **手動から自動へ**: まず手動で価値を実感 → 自動化で楽にする順序
- **既存の仕組みを壊さない**: 予測機能はそのまま維持、記録機能を徐々に統合
- **データはローカル優先**: API連携してもローカル保存ベースは維持（プライバシー重視）
- **身近 + 未来の両軸**: 通知で「今」を、予測で「未来」を支える
