# MoneyFlow ロードマップ

## ビジョン

「記録するから予測が正確になる」— 家計簿と予測が一つのストーリーになるアプリ。
毎月の実績が次月の予測の起点になり、使い続けるほど精度が上がる。

## 現状の課題

- **予測と実績が別世界**: 支出を記録しても予測に反映されない
- **ユーザーの疑問**: 「何のために支出を入力しているの？」
- **残高のズレ**: 手動で1回入れた残高から、設定値ベースで予測するだけ → 現実とのギャップが月ごとに広がる
- **予算外の支出に対応できない**: 臨時支出（冠婚葬祭、家電故障など）が予測に反映されない

---

## 核心機能: 月次確定サイクル

Excelで毎月シートを作り直していたのと同じことを、アプリが自動で行う。

```
月初            月中                月末
 │              │                  │
 ▼              ▼                  ▼
実残高で開始 → 支出記録が予測に反映 → 月を確定 → 次月の起点になる
```

### 3つの時間軸

|          | 過去（確定済み） | 当月（ハイブリッド）            | 未来（予測）    |
|----------|:---:|:---:|:---:|
| 残高     | 実績値           | 実績 + 残り日数の予測          | 設定値ベース   |
| 収入     | 実績値           | 実績 or 設定値                 | 設定値         |
| 支出     | 記録済み実績     | 記録済み + 残り予測            | 設定値         |

### 当月の残高計算（リアルタイム）

```
今月の予測残高 = 月初確定残高
              + 今月の収入（実績 or 設定値）
              - 記録済み支出
              - 残り日数の予測支出（設定値ベース）
```

→ **支出を記録するたびに予測が動く。記録する意味が生まれる。**

### 月末確定フロー

1. 月末（or 翌月初）に「今月を確定」ボタンが出現
2. 実際の残高を入力（銀行残高と照合）
3. 記録済み支出との差額 = 「把握外の支出」として自動計算
   - 「あなたの知らない支出が ¥X ありました」と表示
   - その他カテゴリに分類 or ユーザーが振り分け
4. 確定残高が翌月の起点になる
5. 確定月は編集ロック（誤操作防止）

### 予算外・臨時支出

- 「今月だけの支出」として追加可能
- 即座に当月の予測残高に反映
- 来月以降には影響しない
- 例: 結婚式ご祝儀、家電買い替え、医療費

---

## ~~Phase 1: 通知機能~~ ✅ 完了

~~予測アプリだけでなく「今の使い方、大丈夫？」を教えてくれる存在にする。~~

- ~~ローカル通知（Service Worker + Notification API）で実装済み~~
- ~~予算消化率、週間超過、赤字予測の3種類の通知~~

---

## ~~Phase 2: 月次確定サイクルの実装~~ ✅ 完了

- ~~月末確定フロー（確定ボタン + 実残高入力 + 差額表示）~~
- ~~確定月の編集ロック（誤操作防止）+ 取り消し機能~~
- ~~確定残高 → 翌月起点への自動引き継ぎ~~
- ~~臨時支出: 既存の支出入力で対応可能なため、専用UIはスキップ~~

---

## ~~Phase 3: 予測精度の自動改善~~ ✅ 完了

- ~~変動費の予測を直近3ヶ月の実績平均に自動提案（ユーザー承認制）~~
- ~~カテゴリ別の予測 vs 実績トレンドグラフ（SVG折れ線チャート）~~
- ~~予測精度スコア（リングメーター + カテゴリ別バー）~~

---

## 品質改善 ✅ 完了

- ~~テーマカラーのライト/ダーク両対応~~
- ~~ダークモードでのハードコード色をCSS変数に統一~~
- ~~アクセシビリティ改善（role="dialog"、aria-label追加）~~
- ~~エラー処理強化（FileReader、インポートバリデーション）~~
- ~~sw.jsキャッシュバージョン更新（v5）~~

---

## Phase 4: 残高の自動連携

- 銀行口座の残高を手動 or API で取得し、月次確定を簡略化
- クレジットカードの利用明細を取り込み、支出を自動分類
- サブスク・アプリ課金の自動検出

### API連携の候補（将来）
- マネーフォワード / Zaim などの家計簿API
- 銀行オープンAPI（全銀協標準）
- クレジットカード明細（スクレイピング or API）
- Apple / Google のサブスク管理API

### カード反映の遅延問題
- クレカの利用明細は反映に数日〜数週間かかる
- → 手動入力 + 後からカード明細で突合・修正する運用が現実的
- → サブスクBoxのように「プリセット + 手動」でUXを磨く方向もあり

---

## データ保存の移行計画

### 現状の問題
localStorage はブラウザPWAでは問題ないが、Capacitor（WebView）では
アプリ強制終了時にデータ消失するリスクがある。

### 移行ステップ
1. **今**: localStorage のまま（ブラウザPWA用途では十分）
2. **アプリ化時**: Capacitor Preferences に移行（ネイティブKV保存、安全）
3. **データ量増加時**: IndexedDB or SQLite に移行（大量の支出記録に対応）

### 移行時の注意
- 既存ユーザーの localStorage データを新方式にマイグレーションする処理が必要
- エクスポート/インポート機能は引き続き JSON 形式で維持
- 月次確定データの構造: `{ "2026-05": { balance: 150000, settled: true, income: {...}, expenses: [...] } }`

---

## 設計方針

- **記録 = 予測の精度向上**: 支出を記録する行為が予測に直結する体験
- **段階的に進める**: 各Phaseは独立してリリース可能にする
- **手動から自動へ**: まず手動で価値を実感 → 自動化で楽にする順序
- **既存の仕組みを壊さない**: 予測機能はそのまま維持、記録機能を徐々に統合
- **データはローカル優先**: API連携してもローカル保存ベースは維持（プライバシー重視）
- **身近 + 未来の両軸**: 通知で「今」を、予測で「未来」を支える
